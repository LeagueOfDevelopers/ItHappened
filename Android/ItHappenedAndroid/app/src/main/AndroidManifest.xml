<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ithappenedandroid">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".Retrofit.ItHappenedApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.SignInActivity"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".Activities.UserActionsActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".Activities.AddNewTrackingActivity"
            android:theme="@style/Theme.MyBack">
        </activity>
        <activity android:name=".Activities.AddNewEventActivity"
            android:theme="@style/Theme.MyBack">
        </activity>

        <activity android:name=".Activities.EventDetailsActivity"
            android:theme="@style/Theme.MyBack">
        </activity>

        <activity android:name=".Activities.EditEventActivity"
            android:theme="@style/Theme.MyBack"></activity>

        <activity android:name=".Activities.EditTrackingActivity"
            android:theme="@style/Theme.MyBack"></activity>

        <activity android:name=".Activities.EventsForTrackingActivity"
            android:theme="@style/Theme.MyBack"></activity>

        <activity android:name=".Activities.SplashScreenActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

    </application>

</manifest>